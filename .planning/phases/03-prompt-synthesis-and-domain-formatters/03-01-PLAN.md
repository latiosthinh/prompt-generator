# Plan 03-01: Prompt Synthesis & Domain Formatters

## Goal
Implement `/api/generate-prompt` route with domain-specific synthesis rules and streaming response, and create `PromptViewer` component with syntax formatting, one-click copy, and file export.

## Requirements
- SYN-01: Domain-specific prompt synthesis (Image, Code, Writing, System Prompt, General).
- SYN-02: Structured prompt viewer with copy button and `.txt` / `.md` export.
- SYN-03: Real-time streaming output support with loading animations.

## Tasks
1. Implement `src/app/api/generate-prompt/route.ts` with domain-tailored system instructions and streaming text output.
2. Build `src/components/PromptViewer.tsx` featuring:
   - Streamed typing animation & rendered markdown/preformatted text.
   - Copy to clipboard with success indicator.
   - Download as `.md` and `.txt`.
   - Domain-specific badges (e.g. Midjourney flags, Python/TS specs).
3. Connect `PromptViewer` with `QuestionnaireForm` and `page.tsx`.
4. Verify with build test.
