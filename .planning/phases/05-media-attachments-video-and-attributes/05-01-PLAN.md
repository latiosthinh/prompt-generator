---
phase: 05-media-attachments-video-and-attributes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/schemas.ts
  - src/config/domains.ts
  - src/components/DomainSelector.tsx
  - src/i18n/en.ts
  - src/i18n/vi.ts
  - src/app/api/deconstruct-media/route.ts
  - src/app/api/generate-prompt/route.ts
  - src/app/api/generate-questions/route.ts
autonomous: true
requirements:
  - MEDIA-02
  - VID-01
  - ATTR-01

must_haves:
  truths:
    - "System recognizes Video Generation domain with custom camera, motion, and cinematic video rules"
    - "API accepts file attachments metadata and pinned attribute selections"
    - "/api/deconstruct-media reverse-engineers prompt concepts and attributes from attached text and images"
    - "/api/generate-prompt synthesizes prompts incorporating pinned attributes (ratio, resolution, motion, camera) and video domain rules"
  artifacts:
    - path: "src/types/schemas.ts"
      provides: "AttachmentSchema, PinnedAttributesSchema, updated request/response types"
      contains: "AttachmentSchema"
    - path: "src/config/domains.ts"
      provides: "Video generation domain configuration and pinned attribute options"
      contains: "video-generation"
    - path: "src/app/api/deconstruct-media/route.ts"
      provides: "Media deconstruction route handler for reverse prompting"
      exports: ["POST"]
    - path: "src/app/api/generate-prompt/route.ts"
      provides: "Video domain rules and pinned attributes formatting"
      contains: "DOMAIN RULES (VIDEO GENERATION)"
  key_links:
    - from: "src/app/api/generate-prompt/route.ts"
      to: "src/types/schemas.ts"
      via: "GeneratePromptRequestSchema"
      pattern: "pinnedAttributes"
    - from: "src/app/api/deconstruct-media/route.ts"
      to: "src/lib/mimo.ts"
      via: "mimoClient.chat.completions.create"
      pattern: "mimoClient"
---

<objective>
Implement schemas, domain configurations, i18n dictionaries, and backend API routes supporting video prompt generation, pinned domain attributes (aspect ratio, resolution, motion, camera), and reverse prompt extraction from attached files.

Purpose: Provide robust data contracts and LLM backend processing for media attachments, video domain synthesis, and attribute pin presets before UI wiring.
Output: Updated Zod schemas, video domain configs, `/api/deconstruct-media` route, and enhanced prompt/question synthesis routes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-media-attachments-video-and-attributes/05-CONTEXT.md

<interfaces>
<!-- Existing schemas from src/types/schemas.ts -->
export const UserAnswerSchema = z.object({
  questionId: z.string(),
  selectedOptionIds: z.array(z.string()).default([]),
  customText: z.string().optional(),
});
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Schemas, Video Domain Config, i18n Dictionaries, and Icon Map</name>
  <files>src/types/schemas.ts, src/config/domains.ts, src/components/DomainSelector.tsx, src/i18n/en.ts, src/i18n/vi.ts</files>
  <action>
    1. In `src/types/schemas.ts` (per D-01, D-04, D-05):
       - Define `AttachmentSchema`: `{ id: string, name: string, type: string, size: number, dataUrl?: string, textContent?: string }`.
       - Define `PinnedAttributesSchema`: `{ aspectRatio?: string, resolution?: string, motion?: string, camera?: string, stylePreset?: string, fps?: string, lighting?: string }`.
       - Update `GenerateQuestionsRequestSchema` and `GeneratePromptRequestSchema` to accept optional `attachments: z.array(AttachmentSchema)` and `pinnedAttributes: PinnedAttributesSchema`.
       - Update `SessionRoundSchema` to store `attachments` and `pinnedAttributes`.
       - Define `DeconstructMediaRequestSchema` and `DeconstructMediaResponseSchema` (`{ seed: string, suggestedDomain?: string, detectedAttributes?: PinnedAttributesSchema, breakdown?: { subject?: string, style?: string, lighting?: string, camera?: string, composition?: string } }`).
    2. In `src/config/domains.ts` (per D-02, D-04):
       - Add `video-generation` domain configuration with Video icon name (`Video`), description, placeholder, and examples (e.g. cinematic drone shot, anime action sequence, hyperrealistic time-lapse).
       - Define pinned attribute options per domain (e.g. `DOMAIN_PINNED_ATTRIBUTES` mapping domain ID to configurable radio groups like Ratio: `16:9`, `9:16`, `1:1`, `4:3`, `21:9`; Resolution: `HD`, `FullHD`, `2K`, `4K`; Video Motion: `Subtle`, `Smooth`, `Dynamic`, `Hyper-speed`; Camera: `Static`, `Pan/Tilt`, `Tracking`, `FPV Drone`, `Orbit`).
    3. In `src/components/DomainSelector.tsx`:
      - Import `Video` from `lucide-react` and add it to the hardcoded `ICON_MAP` so the new `video-generation` domain renders (ICON_MAP is typed `Record<DomainConfig['iconName'], LucideIcon>` — a missing key fails typecheck/runtime).
   4. In `src/i18n/en.ts` and `src/i18n/vi.ts`:
       - Add translations for Video domain, file attachments (drag & drop, upload button, break prompt, max size hint), and pinned attribute labels/options in both languages.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Zod schemas export Attachment and PinnedAttributes types, Video domain is configured with translations, and typescript builds with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Media Deconstruction & Enhanced Prompt Synthesis API Routes</name>
  <files>src/app/api/deconstruct-media/route.ts, src/app/api/generate-prompt/route.ts, src/app/api/generate-questions/route.ts</files>
  <action>
    1. Create `src/app/api/deconstruct-media/route.ts` (per D-03):
       - Accepts `attachments` (text content, file metadata, image descriptions) and optional initial prompt.
        - Prompts Xiaomi-MiMo / LLM to deconstruct the media into a rich seed concept, extracting key artistic dimensions (subject, medium, lighting, camera movement, composition, aspect ratio).
        - Image attachments with `dataUrl` are sent using the OpenAI vision message format: content array entry `{ type: "image_url", image_url: { url: dataUrl } }`. Attachments without `dataUrl` (text files via `textContent`, metadata-only) fall back to a plain text content entry describing name/type/size plus extracted text.
       - Returns structured JSON payload matching `DeconstructMediaResponseSchema`.
    2. Enhance `src/app/api/generate-prompt/route.ts` (per D-02, D-04):
       - Add `### DOMAIN RULES (VIDEO GENERATION)` covering camera trajectories (pan, tilt, zoom, tracking, orbit, crane), motion speed/physics, frame rates (24fps cinematic, 60fps fluid), temporal consistency, sound cues, and AI video generator formats (Runway Gen-3, Kling, Sora, Luma, Pika).
       - Inject `pinnedAttributes` (aspect ratio, resolution, motion, camera, render style) into the user prompt context and final parameters section.
       - If `attachments` are present, format attachment context for LLM prompt synthesis.
    3. Enhance `src/app/api/generate-questions/route.ts`:
       - Handle `video-generation` domain context and incorporate selected `pinnedAttributes` and `attachments` so generated clarification questions do not duplicate already-pinned attributes.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All routes validate request bodies with Zod, support Video domain rules, process media attachments, and compile cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> /api/deconstruct-media | Untrusted file metadata and text payloads |
| Client -> /api/generate-prompt | Untrusted seed, pinned attributes, and attachment data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | /api/deconstruct-media | mitigate | Validate request schema with Zod, cap text payload length to 10KB to prevent prompt injection / buffer overflow. |
| T-05-02 | Denial of Service | /api/generate-prompt | mitigate | Limit attachment arrays and sanitize pinned attribute strings through strict Zod schemas. |
</threat_model>

<verification>
Automated TypeScript compilation check: `npx tsc --noEmit`
</verification>

<success_criteria>
- Video domain and pinned attribute configurations exist and are fully localized in en and vi.
- Media deconstruction route cleanly parses attachments and returns structured reverse prompt analysis.
- Prompt generation route properly synthesizes video prompts and incorporates pinned radio parameters.
</success_criteria>

<output>
After completion, create `.planning/phases/05-media-attachments-video-and-attributes/05-01-SUMMARY.md`
</output>
