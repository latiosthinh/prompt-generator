---
phase: 05-media-attachments-video-and-attributes
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/components/SeedInput.tsx
  - src/components/PinnedAttributesSelector.tsx
  - src/app/page.tsx
  - src/lib/storage.ts
autonomous: true
requirements:
  - MEDIA-01
  - VID-01
  - ATTR-01

must_haves:
  truths:
    - "User can attach files via file picker or drag-and-drop onto the seed input area with preview chips"
    - "Pinned attribute radio/chip selectors render per domain and flow into generation requests"
    - "User can trigger reverse prompt deconstruction of attachments to populate seed and detected attributes"
    - "Attachments and pinned attributes persist across reloads via session storage"
  artifacts:
    - path: "src/components/SeedInput.tsx"
      provides: "Drag-and-drop zone, file picker, attachment preview chips"
      contains: "onDrop"
    - path: "src/components/PinnedAttributesSelector.tsx"
      provides: "Radio/chip preset bar driven by DOMAIN_PINNED_ATTRIBUTES"
      contains: "PinnedAttributesSelector"
    - path: "src/app/page.tsx"
      provides: "State + wiring of attachments and pinnedAttributes into API payloads and storage"
      contains: "pinnedAttributes"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/api/generate-prompt/route.ts"
      via: "fetch"
      pattern: "pinnedAttributes"
    - from: "src/components/PinnedAttributesSelector.tsx"
      to: "src/config/domains.ts"
      via: "DOMAIN_PINNED_ATTRIBUTES"
      pattern: "DOMAIN_PINNED_ATTRIBUTES"
    - from: "src/app/page.tsx"
      to: "src/app/api/deconstruct-media/route.ts"
      via: "fetch"
      pattern: "deconstruct-media"
---

<objective>
Deliver the client-side experience for Phase 5: drag-and-drop file attachments with previews, per-domain pinned attribute radio/chip selectors, reverse-prompt deconstruction trigger, and full state wiring into API payloads and hydration-safe local persistence.

Purpose: Make backend contracts from plan 01 usable through the UI per decisions D-01, D-03, D-04, D-05.
Output: Updated SeedInput with dropzone, new PinnedAttributesSelector component, page.tsx state wiring, storage persistence.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-media-attachments-video-and-attributes/05-CONTEXT.md
@.planning/phases/05-media-attachments-video-and-attributes/05-01-PLAN.md

<interfaces>
<!-- Schemas added by plan 01 -->
AttachmentSchema: { id, name, type, size, dataUrl?, textContent? }
PinnedAttributesSchema: { aspectRatio?, resolution?, motion?, camera?, stylePreset?, fps?, lighting? }
DOMAIN_PINNED_ATTRIBUTES: Record<domainId, PinnedAttributeGroup[]> where each group = { key, labelKey, options: { value, labelKey }[] }

<!-- Existing component pattern (SeedInput.tsx) -->
'use client'; components take `t: Translations` prop, use Tailwind with vintage palette (#C15F3D, #E6DFD3, #2B2520...), lucide-react icons.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attachments UI — Drag-Drop Zone and Preview Chips in SeedInput</name>
  <files>src/components/SeedInput.tsx</files>
  <action>
    1. Extend `SeedInputProps` with `attachments: Attachment[]`, `onChangeAttachments: (next: Attachment[]) => void`, and `onDeconstruct: () => void` plus `isDeconstructing: boolean` (D-01, D-03).
    2. Add a hidden file input (`accept="image/png,image/jpeg,image/webp,.txt,.md,.json"`) plus an attach button (lucide `Paperclip`).
    3. Implement `onDragOver`/`onDrop` handlers on the seed input wrapper: accept dropped files, encode images as data URLs via `FileReader.readAsDataURL`, read text files (`.txt`, `.md`, `.json`) via `readAsText`, enforce max file size (2MB per file, max 5 files) with i18n error hints, reject unsupported types.
    4. Render attachment preview chips below the textarea: thumbnail (`next/image` or `img` with dataUrl) for images, file icon + truncated name for text, size label, and remove (`X`) button per chip.
    5. Add a "Break prompt from files" button (lucide `ScanSearch` or `Wand2`) enabled when attachments exist; calls `onDeconstruct`, shows Loader2 spinner while `isDeconstructing`.
    6. Use i18n keys added in plan 01 (`t.seedInput.attach*`, `t.seedInput.deconstruct*`).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>SeedInput accepts files via picker and drag-drop with previews, size limits, removal, and a working deconstruct trigger, compiling cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: PinnedAttributesSelector Component</name>
  <files>src/components/PinnedAttributesSelector.tsx</files>
  <action>
    1. Create `src/components/PinnedAttributesSelector.tsx` as a `'use client'` component (D-04).
    2. Props: `domainId: string`, `value: PinnedAttributes`, `onChange: (next: PinnedAttributes) => void`, `disabled?: boolean`, `t: Translations`.
    3. Read `DOMAIN_PINNED_ATTRIBUTES[domainId]` from `src/config/domains.ts`; render nothing when the domain has no pinned attribute groups.
    4. Render one group per attribute (Ratio, Resolution, Motion, Camera, ...) as a labeled row of radio-style chip buttons (single-select within group, re-click clears selection); selected chip uses the accent style (`#C15F3D` / dark `#DA7756`) matching existing chip styles in SeedInput examples.
    5. Keyboard accessible: use `button type="button"` with `aria-pressed` state.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>PinnedAttributesSelector renders per-domain radio chip groups, toggles values immutably, and compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Page Wiring and Persistence</name>
  <files>src/app/page.tsx, src/lib/storage.ts</files>
  <action>
    1. In `src/app/page.tsx` (D-05):
       - Add state: `attachments: Attachment[]` and `pinnedAttributes: PinnedAttributes`.
       - Render `PinnedAttributesSelector` between `DomainSelector` and `SeedInput` (or below seed input), passing selected domain id; reset `pinnedAttributes` on domain switch.
       - Pass attachment props and `onDeconstruct` handler into `SeedInput`.
       - Implement `handleDeconstruct`: POST `/api/deconstruct-media` with current attachments (+ seed if present); on success populate `seed` from `seed`, merge `detectedAttributes` into `pinnedAttributes`; surface errors via existing error state.
       - Include `attachments` (strip `dataUrl` before sending to keep payloads small; keep `textContent`) and `pinnedAttributes` in `/api/generate-questions` and `/api/generate-prompt` request bodies when present.
    2. In `src/lib/storage.ts`:
       - Extend the persisted session shape with `attachments` (data URLs stripped, metadata + textContent only) and `pinnedAttributes`; keep existing LRU pruning and hydration-safe patterns unchanged.
       - Save on answer/round changes like existing fields; restore on session load into page state.
    3. Keep all text through `t.*` i18n keys added in plan 01.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Page pipes attachments and pinned attributes into both API calls and persistence; deconstruction populates seed and attributes; reload restores state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dropped/picked files -> browser | Untrusted local files; size/type validation before encoding |
| /api/deconstruct-media response -> DOM | LLM output rendered into seed/attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | SeedInput dropzone | mitigate | Enforce 2MB per file and 5-file cap client-side before FileReader; reject unsupported MIME types. |
| T-05-04 | Information Disclosure | storage.ts | mitigate | Strip image `dataUrl` payloads before persisting to LocalStorage to avoid quota exhaustion and data bloat. |
| T-05-05 | Tampering | page.tsx | mitigate | Treat deconstruct-media response as untrusted data: assign via React state only (no dangerouslySetInnerHTML), validate with client-side Zod schemas from plan 01. |
</threat_model>

<verification>
Automated TypeScript compilation check: `npx tsc --noEmit`
</verification>

<success_criteria>
- Files attach via picker and drag-drop with previews, limits, and removal.
- Pinned attribute chips appear per domain and single-select within groups.
- Deconstruct button fills seed + attributes from attachments.
- Attachments (metadata/text) and pinned attributes survive reload via session storage.
</success_criteria>

<output>
After completion, create `.planning/phases/05-media-attachments-video-and-attributes/05-02-SUMMARY.md`
</output>
